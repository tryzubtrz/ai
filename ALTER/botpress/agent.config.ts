import { defineConfig } from '@botpress/runtime'

export default defineConfig({
  name: 'alter',
  description:
    'ALTER — Universal Digital Twin. Owner-controlled orchestration layer with policy, secret, approval and task-completion boundaries.',
})
