services:
  - type: worker
    name: mexc-signal-bot
    runtime: python
    rootDir: mexc_signal_bot
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
